/*
 * UpcomingDonations Messages
 *
 * This contains all the text for the UpcomingDonations container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UpcomingDonations';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpcomingDonations container!',
  },
});
