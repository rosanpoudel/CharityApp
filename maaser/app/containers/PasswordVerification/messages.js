/*
 * PasswordVerification Messages
 *
 * This contains all the text for the PasswordVerification container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PasswordVerification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PasswordVerification container!',
  },
});
