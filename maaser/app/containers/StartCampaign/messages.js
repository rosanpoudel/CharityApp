/*
 * StartCampaign Messages
 *
 * This contains all the text for the StartCampaign container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StartCampaign';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StartCampaign container!',
  },
});
