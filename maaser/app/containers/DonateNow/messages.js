/*
 * DonateNow Messages
 *
 * This contains all the text for the DonateNow container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DonateNow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DonateNow container!',
  },
});
