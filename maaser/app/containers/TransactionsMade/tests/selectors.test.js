// import { selectTransactionsMadeDomain } from '../selectors';

describe('selectTransactionsMadeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
