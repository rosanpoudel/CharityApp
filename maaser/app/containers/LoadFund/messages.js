/*
 * LoadFund Messages
 *
 * This contains all the text for the LoadFund container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoadFund';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoadFund container!',
  },
});
