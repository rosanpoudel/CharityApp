/*
 * ScheduleDonation Messages
 *
 * This contains all the text for the ScheduleDonation container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ScheduleDonation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ScheduleDonation container!',
  },
});
