// import { selectScheduleDonationDomain } from '../selectors';

describe('selectScheduleDonationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
