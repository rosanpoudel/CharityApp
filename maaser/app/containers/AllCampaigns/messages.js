/*
 * AllCampaigns Messages
 *
 * This contains all the text for the AllCampaigns container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllCampaigns';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllCampaigns container!',
  },
});
