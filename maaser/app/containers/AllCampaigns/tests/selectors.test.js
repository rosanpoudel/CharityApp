// import { selectAllCampaignsDomain } from '../selectors';

describe('selectAllCampaignsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
