/*
 * SiteLayoutScreen Messages
 *
 * This contains all the text for the SiteLayoutScreen container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SiteLayoutScreen';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SiteLayoutScreen container!',
  },
});
