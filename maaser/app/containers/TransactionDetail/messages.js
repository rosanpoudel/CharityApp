/*
 * TransactionDetail Messages
 *
 * This contains all the text for the TransactionDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransactionDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionDetail container!',
  },
});
