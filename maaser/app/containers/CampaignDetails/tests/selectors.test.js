// import { selectCampaignDetailsDomain } from '../selectors';

describe('selectCampaignDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
