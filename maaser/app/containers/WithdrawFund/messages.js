/*
 * WithdrawFund Messages
 *
 * This contains all the text for the WithdrawFund container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WithdrawFund';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WithdrawFund container!',
  },
});
