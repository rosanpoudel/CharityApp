// import { selectWithdrawFundDomain } from '../selectors';

describe('selectWithdrawFundDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
