// import { selectSubcampaignDetailsDomain } from '../selectors';

describe('selectSubcampaignDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
