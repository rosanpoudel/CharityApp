/*
 * SubcampaignDetails Messages
 *
 * This contains all the text for the SubcampaignDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubcampaignDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubcampaignDetails container!',
  },
});
